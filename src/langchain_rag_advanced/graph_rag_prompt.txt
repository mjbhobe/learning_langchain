These are fantastic examples, especially the "The "Corporate Interconnect" (Financial Analysis)" examples, which clearly illustrated the need for a GraphDB to link the inter-connections between companies (for example Apple & TSMC) and answer questions like "How does a chip shortage in Taiwan affect Apple's revenue?"

Now I want you to write a technical blog, along with code snippets AND the complete code for the above example.

First we'll develop the Python project fully.

My preferred tech-stack:
- Python 3.12+ and uv for managing local Python environment 
- LangChain (2.14+) or LangGraph
- OpenAI (gpt-5-nano) for LLM
- OpenAIEmbeddings (embeddings)
- ChromaDB (vector store)
- Neo4j (free community edition to create/save local graph DB)
-  Any other Python libraries (modules) to load & process all the 10-Ks and any other financial data
- CONSTRAINT (MUST comply!) 
    - All software in tech stack, including all Python modules and data sources you use, must be free to use. Data must be publicly available (such as 10Ks from SEC website). 
    - All System Prompts and user-prompts must be optimized to minimize token usage of OpenAI gpt-5-nano. I don't want to blow someones preloaded tokens!

How we should develop the complete code project:
- Assume we'll be developing the project in the ~/code/corp_intercon folder
- Start by giving instructions on how to create the local uv environment (in .venv folder using Python 3.12+) - it should create a pyproject.toml file too
- Next show me how to activate the local environment and install all required modules [uv add... command(s)]
- Then show me where to download all the relevant 10-ks from (let's go with list of all inter related tech companies such as  Apple, Nvidia, TSMC, ASML, Microsoft, Anthropic, AWS and Google). Prefer to download the latest 10Ks
- Then show Python code (preferably a Python function(s)) to create a vectorDB from all the downloaded 10Ks - we'll use OpenAIEmbeddings and ChromaDB to offline store the embeddings to chroma_db/10ks subfolder. 
- As a first step, we'll go with semantic search and show how simple queries work beautifully, but queries that interconnect entities (e.g. "How does a chip shortage in Taiwan affect Apple's revenue?") will fail with a vectorDB only RAG.
    - Develop a separate Python program that runs an infinite loop that queries just the vectorDB you just built. This loop will ask for user query -> retrieve relevant context from vectorDB -> generate response from LLM and show the user (the usual simple Naive RAG pipeline). The loop should exit if user types "exit" or "quit", else loop infinitely.
     - Show me 5 examples of queries that will work (simple or medium complex queries guaranteed to work!) and 5 examples of queries (such as interconnected queries above or more complex queries guaranteed to fail with just the vectorDB).
- As a next step, we'll develop the GraphDB using Neo4j that will automatically extract all the entities and relations between them. I prefer not to manually do this - LangChain modules should be able to handle this (I presume). Show me complete code to build & save offline GraphDB, preferably as a Python function
- Develop a separate Python program that runs an infinite loop that queries just the graphDB you just built - I will enter the text query, which should be converted to cipher (text-to-cipher), which extracts relevant context from the NodeJS graph db we built. This loop will ask for user query -> retrieve relevant context from graphDB -> generate response from LLM and show the user (the usual simple Naive RAG pipeline). The loop should exit if user types "exit" or "quit", else loop infinitely. Here we will test with simple and inter-connected queries to show that both work
- Next develop a separate Python program that runs an infinite loop that queries BOTH the vectorDB and graphDB for context and responds to user's query. If user types "quit' or "exit" only then does the infinite loop quit.

Finally, let's write a technical blog to be published on Medium.com or LinkedIn using a casual technical style. It should have the following structure
- Need for RAG: explain very briefly the need for RAG